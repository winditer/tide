- **环境配置**：必须设置 `LARK_APP_ID` 和 `LARK_APP_SECRET` 环境变量，或通过根目录 `.env` 文件注入；支持通过 `CODEX_BIN`、`CLAUDE_BIN`、`QODER_BIN` 自定义各 Agent 的执行路径。
- **启动方式**：直接运行 `python tide_ws.py`，程序会自动加载配置并启动 WebSocket 监听，首次运行需确保 `LARK_ALLOWED_CHAT_IDS` 已配置以通过权限校验。