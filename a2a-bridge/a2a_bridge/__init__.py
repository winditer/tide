"""Tide A2A Bridge — packaging wrapper.

The actual server code lives in the root-level modules (main.py, config.py,
executor.py, etc.).  This package provides:

* ``a2a-bridge`` CLI entry point (setup / start / stop / status)
* ``python -m a2a_bridge`` runner
"""

__version__ = "1.0.0"
