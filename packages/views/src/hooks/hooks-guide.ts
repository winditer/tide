// Hooks usage guide — rendered by HooksGuide.tsx via SimpleMarkdown.
export const HOOKS_GUIDE = `# 事件钩子使用指南

## 钩子的作用
事件钩子（Hooks）让系统在特定事件发生时，自动调用你指定的外部服务。比如任务一完成就自动发飞书通知、工作项完成后自动触发 CI 部署。它是把 Tide 和你现有工具链打通的桥梁。

## 什么时候用
- 想在任务/工作项状态变化时，把消息推送到飞书、钉钉、企业微信等
- 想在某类工作项完成后，自动触发外部系统（如构建、部署、同步）
- 想把系统事件同步到自己的监控或数据平台

## 支持哪些事件
- 工作项创建 / 更新 / 完成 / 分配
- 任务完成 / 任务失败
- 审批待处理 / 审批已处理

## 如何添加 Webhook 钩子
1. 进入"钩子"页面，点击"创建钩子"
2. 填写钩子名称，方便识别用途
3. 选择要监听的事件（如"任务完成"）
4. 选择执行方式为 Webhook，填入接收通知的回调地址（URL）
5. 按目标服务的要求填写请求方式和消息内容模板（可用事件里的字段作为变量动态填充）
6. 打开"启用"开关并保存

## 如何配置过滤条件
如果不希望某个事件的每一次发生都触发钩子，可以加过滤条件进一步收窄范围，例如：
- 只针对某个项目的事件触发
- 只针对带特定标签（如 release）的工作项触发

配置后，只有同时满足事件类型和过滤条件的实例才会调用钩子。

## 测试和调试钩子
- 保存钩子后，可先在测试环境用真实事件触发一次，确认目标服务能正确收到
- 检查回调地址是否可访问、响应是否及时
- 在钩子列表查看每次执行的状态，若失败则根据记录排查地址、请求格式或权限问题
- 调试通过后再在生产环境启用

## 使用案例

### 案例 1：任务完成后通知飞书群
**场景**：Agent 任务执行完成后，自动向飞书群发送通知
- **事件类型**：task.completed
- **钩子类型**：Webhook
- **回调地址**：飞书机器人 Webhook URL
- **消息内容**：「✅ 任务《{任务标题}》已完成，执行耗时 {时长}，查看结果：{链接}」
- **效果**：团队实时感知任务进展，无需频繁刷新页面

### 案例 2：工作项创建后同步到外部系统
**场景**：工作项创建时自动同步到 Jira / 禅道等外部项目管理系统
- **事件类型**：work_item.created
- **钩子类型**：Webhook
- **回调地址**：外部系统的 API 接口
- **过滤条件**：仅同步特定项目的工作项
- **效果**：双系统数据自动同步，避免手动录入

### 案例 3：审批待处理时发送邮件提醒
**场景**：审批节点产生待处理审批时，邮件通知审批人
- **事件类型**：approval.pending
- **钩子类型**：Webhook
- **回调地址**：邮件发送服务接口
- **消息内容**：包含工作项标题、申请人、审批链接
- **效果**：审批人即使不在系统内也能及时收到提醒

### 案例 4：任务失败时触发告警
**场景**：Agent 任务执行失败时，发送告警到监控系统
- **事件类型**：task.failed
- **钩子类型**：Webhook
- **回调地址**：PagerDuty / 企业微信告警接口
- **过滤条件**：仅生产环境项目
- **消息内容**：错误信息、失败任务 ID、所属工作项
- **效果**：生产问题及时发现，快速响应

### 案例 5：工作项完成后触发 CI/CD 部署
**场景**：工作项标记完成后，自动触发对应分支的构建部署
- **事件类型**：work_item.completed
- **钩子类型**：Webhook
- **回调地址**：CI/CD 系统触发接口（如 Jenkins、GitHub Actions）
- **过滤条件**：带有"需部署"标签的工作项
- **效果**：开发完成到部署上线全自动化

## 注意事项与最佳实践
- 确保回调地址稳定可访问且响应快，避免钩子长时间等待
- 为钩子配置合理的过滤条件，避免无关事件频繁触发
- 善用模板变量动态注入事件数据，让通知内容更有用
- 持续关注钩子执行状态，及时处理连续失败的钩子
`;
