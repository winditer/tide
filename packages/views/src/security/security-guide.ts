// Security (ECC) usage guide — rendered by SecurityGuide.tsx via SimpleMarkdown.
export const SECURITY_GUIDE = `# 安全审查（ECC）使用指南

## 安全扫描做什么
安全审查模块（ECC）会在 Agent 生成代码后自动扫描，帮你发现潜在的安全风险，比如硬编码的密钥、危险的删除命令、注入漏洞等。它相当于给 AI 产出的代码加了一道自动安检，避免风险代码悄悄进入项目。

## 它能识别哪些风险
- **敏感信息泄露**：API Key、密码、Token、私钥等被硬编码
- **危险操作**：rm -rf、DROP TABLE、格式化等可能造成数据丢失的命令
- **代码注入**：SQL 注入、XSS、命令注入等
- **合规问题**：许可证、数据隐私等合规检查

## 风险等级说明
每条安全发现都会标注严重程度，帮你判断处理优先级：
- **Critical（严重）**：必须立即处理，通常会即时告警
- **High（高）**：优先处理
- **Medium（中）**：择期处理
- **Low（低）**：可关注，风险较小

## 如何查看安全发现
1. 进入"安全"页面，打开安全发现列表
2. 可按风险等级、状态、项目等条件筛选
3. 点开某条发现，查看命中的规则、涉及的代码位置和修复建议
4. 在统计概览中查看整体的安全趋势和分布

## 如何处理安全告警
安全发现的处理流程如下：
1. Agent 输出代码后自动触发扫描
2. 命中规则时生成一条安全发现，并按严重程度告警
3. 人工审核后，把该发现标记为：**确认风险** / **误报** / **已修复**
4. 处理结果会反映到统计面板，便于持续跟踪

对 Critical/High 的发现应尽快处理；确认是误报的及时标记，避免干扰后续判断。

## 如何管理扫描规则
1. 进入安全规则管理界面，查看现有规则
2. 新建规则时填写规则名称、所属分类、风险等级、匹配方式和修复建议
3. 对已有规则可编辑或删除
4. 需要快速铺开时，可使用批量导入功能一次性导入行业标准规则集

## 使用案例

### 案例 1：检测硬编码的数据库密码
**发现场景**：Agent 在编写数据库配置时直接写入了密码
- **风险等级**：Critical（严重）
- **检测规则**：敏感信息 - 密码/密钥硬编码
- **发现内容**：\`db_password = "MySecret123"\` 出现在配置文件中
- **处理方式**：确认风险 → 修改为环境变量引用 → 标记已修复
- **正确做法**：使用 \`os.environ.get("DB_PASSWORD")\` 替代

### 案例 2：检测危险的文件删除操作
**发现场景**：Agent 生成的脚本中包含 \`rm -rf /\` 类危险命令
- **风险等级**：High（高危）
- **检测规则**：危险操作 - 递归删除
- **发现内容**：\`subprocess.run(["rm", "-rf", path])\` 中 path 来自用户输入
- **处理方式**：确认风险 → 添加路径白名单校验 → 标记已修复
- **建议**：始终校验删除路径范围，禁止操作系统根目录

### 案例 3：检测 SQL 注入风险
**发现场景**：Agent 生成的代码使用字符串拼接构建 SQL
- **风险等级**：High（高危）
- **检测规则**：代码注入 - SQL 注入
- **发现内容**：\`query = f"SELECT * FROM users WHERE name = '{name}'"\`
- **处理方式**：确认风险 → 改用参数化查询 → 标记已修复
- **正确做法**：\`cursor.execute("SELECT * FROM users WHERE name = ?", (name,))\`

### 案例 4：检测未加密的 HTTP 请求
**发现场景**：代码中使用 http:// 而非 https:// 发送敏感数据
- **风险等级**：Medium（中危）
- **检测规则**：合规检查 - 未加密传输
- **发现内容**：\`requests.post("http://api.example.com/login", data=credentials)\`
- **处理方式**：评估是否为内网调用；若为外网则改为 HTTPS
- **建议**：所有包含用户凭证的请求必须使用 HTTPS

### 案例 5：批量导入安全规则
**场景**：新项目启动时一次性导入标准安全规则集
- **操作步骤**：
  1. 进入 Settings → 安全审查
  2. 点击"导入规则"
  3. 选择预置规则集（如 OWASP Top 10、CWE 常见漏洞）
  4. 确认导入，规则立即生效
- **效果**：新项目快速具备安全扫描能力，无需逐条配置

## 注意事项与最佳实践
- 定期更新规则库，覆盖最新的威胁模式
- 结合项目特点自定义规则（如你所用框架的常见漏洞）
- Critical 级别发现不可忽略，务必第一时间处理
- 建立安全基线，持续观察发现趋势，及早发现异常
`;
