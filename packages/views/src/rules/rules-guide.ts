// Guide content for the Rules engine module — keep in sync with product docs.
export const RULES_GUIDE = `# 规则引擎使用指南

## 规则是什么，有什么用
规则引擎让你用"当满足某个条件时，自动做某件事"的方式配置自动化。无需人工盯着，系统会在事件发生时自动分配任务、发送通知或做合规校验，帮团队省去大量重复操作。

## 什么时候用
- 希望特定类型的工作项自动分配给固定成员或专家团
- 希望满足条件（如高优先级完成）时自动通知相关人
- 希望工作项状态变更时自动流转，或字段变更时做合规检查

## 规则能做哪些事
- **自动分配**：按标签、类型等把工作项分给指定成员/团队
- **自动通知**：满足条件时通知项目负责人或指定对象
- **状态流转**：工作项状态变化时自动推进
- **字段校验**：字段变更时做合规检查

## 如何创建规则
1. 进入"规则"页面，点击"创建规则"
2. 填写一个清晰的规则名称，便于日后管理
3. 选择作用域（见下文）
4. 配置触发条件与要执行的动作
5. 保存并启用规则

## 条件和动作怎么配置
**条件**：选择要判断的字段（如标签、优先级、状态），选择比较方式（包含、等于、大于等），填入目标值。需要多个条件同时满足时，可用"且/或"组合。

**动作**：选择条件满足后要执行的操作类型：
- 分配：选择分配目标（成员 / 专家团）
- 通知：选择通知对象（如项目负责人）
- 状态流转 / 字段更新：选择目标状态或字段值

## 作用域如何选择
规则可在三个层级生效，范围由小到大：
- **项目级**：只对某个项目生效，优先级最高
- **项目组级**：对整个项目组生效
- **全局级**：对所有项目生效

当多条规则同时命中时，项目级优先于项目组级、项目组级优先于全局级。

## 常见场景举例
- **前端任务自动分配**：条件"标签 包含 frontend" → 动作"分配给 前端专家团"
- **高优先级完成通知**：条件"优先级 ≥ 高 且 状态 = 已完成" → 动作"通知 项目负责人"
- **发布任务合规校验**：条件"标签 包含 release" → 动作"校验必填字段是否完整"

## 使用案例

### 案例 1：前端任务自动分配给前端团队
**场景**：当工作项包含"前端"标签时，自动分配给前端专家团
- **规则名称**：前端任务自动分配
- **作用域**：项目级
- **条件**：标签包含"frontend"或"前端"
- **动作**：自动分配给前端专家团
- **效果**：新工作项无需手动分配，按标签自动路由到对应团队

### 案例 2：高优先级任务自动通知项目负责人
**场景**：优先级为"紧急"的工作项创建后，立即通知项目负责人
- **规则名称**：紧急任务通知
- **作用域**：项目组级
- **条件**：优先级 >= 紧急（P0/P1）
- **动作**：通知项目 Owner
- **效果**：紧急问题第一时间触达负责人，加快响应速度

### 案例 3：超期工作项自动升级
**场景**：工作项超过截止日期未完成时自动提升优先级
- **规则名称**：超期自动升级
- **作用域**：全局
- **条件**：当前日期 > 截止日期 且 状态 ≠ 已完成
- **动作**：优先级提升一级 + 通知负责人
- **效果**：避免任务被遗忘，自动升级提醒

### 案例 4：特定分支合并后自动创建测试工作项
**场景**：release 分支合并完成后，自动创建回归测试工作项
- **规则名称**：Release 合并触发测试
- **作用域**：项目级
- **条件**：Git Merge 节点完成 且 目标分支为 release/*
- **动作**：创建"回归测试"工作项并分配给 QA 团队
- **效果**：发布流程自动化，不遗漏测试环节

## 注意事项与最佳实践
- 从简单规则开始，跑通后再逐步增加条件复杂度
- 给规则起清晰的名字，方便团队理解和维护
- 留意规则之间可能相互触发，避免形成循环
- 上线前先小范围验证，确认命中范围符合预期
`;
