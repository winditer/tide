// Workflow mode configuration guide — keep in sync with product docs.
export const WORKFLOW_MODE_GUIDE = `# 工作流模式配置指南

## 这是做什么的
工作流模式决定了项目下工作项如何流转。你可以为不同项目选择不同的协作方式：让工作项跑固定的自动化管线，或让团队像看板一样自由拖拽协作。配置支持继承：**项目级 > 项目组级 > 系统默认**，未单独设置时自动向上继承。

## 三种模式怎么选

### 1. 系统默认工作流
沿用管理员配置的全局默认工作流，工作项按预设节点顺序自动流转（如 开发 → 审查 → 测试 → 合并）。
**适合**：流程标准化、希望开箱即用的团队。

### 2. 自定义工作流
为项目单独绑定一个专属工作流模板，节点和顺序可自由编排。
**适合**：有特殊流程需求的项目，如多阶段审批、多分支并行开发。

### 3. 自由协作（Freeform）
没有固定管线，工作项状态由"是否分配、分配后进展"自动派生，也可在看板上手动拖拽：
- 未分配 → 待接受 → 进行中 → 已完成

还支持评论式协作：在工作项里 @专家团 / @小队 即可触发 Agent 执行。
**适合**：敏捷迭代、快速原型、灵活协作的小团队。

## 如何在项目中切换模式
1. 进入项目详情页，点击"设置"Tab
2. 找到"工作流模式"卡片
3. 在三张模式卡片中点击你想要的模式
4. 选择后立即保存生效；选择"自定义工作流"时，下方会出现工作流绑定区域，选定具体工作流即可

## 项目组继承是怎么工作的
- 在项目组详情页可为整个组设置统一的工作流模式
- 组内子项目**未单独配置**时，自动继承项目组的模式
- 一旦子项目在自己的设置里选择了模式，就以子项目为准，不再继承

这样你只需在项目组配置一次，新建的子项目就能自动套用同一套协作方式。

## Freeform 模式的操作方式
切换到自由协作后：
- 直接把工作项分配给成员或专家团，即可推动状态从"待接受"到"进行中"
- 在看板上拖拽工作项到目标状态列，手动调整进度
- 在工作项评论区 @专家团 / @小队，触发 Agent 自动处理

## 状态列配置方法（管理员）
Freeform 看板的状态列是全局统一的，由管理员维护：
1. 进入 Settings → Freeform 状态列管理
2. 添加、删除或拖拽重排序状态列
3. 修改每列的显示名称
4. 保存后，所有 Freeform 项目的看板和筛选会同步更新

## 注意事项与最佳实践
- 优先在项目组统一配置，子项目仅在特殊需求时才单独设置，减少重复维护
- 小团队快速迭代优先用 Freeform；大型、需合规留痕的项目建议用工作流模式
- 状态列命名要简洁明确，数量控制在 4-6 个以内，避免看板过于杂乱
- 切换模式不会丢失已有工作项，但会改变它们的流转与展示方式，切换前先与团队同步
`;
