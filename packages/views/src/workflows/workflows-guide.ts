// Operation-oriented guide for the Workflows feature — focuses on what it does and how to use it.
export const WORKFLOWS_GUIDE = `# 工作流使用指南

## 一、什么是工作流

工作流让你把多个 AI 执行步骤、人工审批和分支操作串成一条**可视化的自动化流程**。相比单次 Agent 对话，工作流能表达"先做什么、再做什么、什么情况下走哪条路"这样的复杂逻辑，并让整个过程自动流转。

### 核心概念

- **节点（Node）**：流程中的一个执行单元，例如让 Agent 干活、等待人工审批、按条件判断走向等；
- **连线（Edge）**：节点之间的箭头，定义执行的先后顺序和走向；
- **执行路径**：从起点节点出发，沿着连线一路走到终点节点，就是一次完整的执行路径。根据条件不同，同一个工作流可能走出不同的路径；
- **运行（Run）**：工作流的一次实际执行，会记录每个节点的状态和产出。

典型使用场景：

- **代码审查流水线**：Agent 生成代码 → 人工审批 → 自动合并分支；
- **多 Agent 协作**：前端、后端、测试并行开发，全部完成后汇合；
- **带条件的流程**：根据 Agent 的分析结果，自动决定是通过还是转去修复；
- **自动化运维**：定时触发巡检工作流，按条件走不同的告警分支。

---

## 二、支持的节点类型

### 起止节点

每个工作流都要有明确的开始和结束标记：

- **Start（开始）**：工作流的唯一入口，每个工作流必须有且仅有一个；
- **End（正常结束）**：表示流程成功完成；
- **Cancel（取消）**：表示流程被主动取消中止；
- **Error（错误）**：表示流程因异常而退出；
- **Close（业务关闭）**：表示业务层面的关闭。

> 一个工作流可以有多个结束节点，让不同的执行路径走向不同的结束语义。结束类节点不能再往外连线。

### Agent 节点（执行节点）

工作流的核心干活单元，调用 AI Agent 完成一项具体任务，例如"生成代码""编写测试""整理文档"。这是使用最频繁的节点。

### 审批节点（Approval）

人工审批门禁。流程走到这里会**暂停并等待人工确认**，审批通过后继续往下走，常用于代码合并、正式发布等关键决策点之前。

### 条件节点（Condition）

按运行时的数据自动判断走哪条路。例如"分析结果是否通过？"——通过走一条路，不通过走另一条路。条件节点通常需要两条出线：满足条件走一条，不满足走另一条（默认分支）。

### 合并节点（Git Merge）

自动执行 Git 分支合并，例如把开发分支合并到发布分支。可配置源分支、目标分支、合并策略以及冲突处理方式，适合放在开发完成、审批通过之后。

### 并行节点（Parallel / Parallel Join）

- **Parallel（并行分发）**：把流程拆成多条同时执行的路径，例如前端、后端、测试同时开工；
- **Parallel Join（并行汇合）**：等待所有并行路径都完成后再继续，通常与并行分发配对使用。

### 其他节点

- **Delay（延时）**：暂停指定时间后再继续，适合等待外部系统（如 CI 构建）处理完成；
- **Stage（阶段标记）**：当工作流与工作项关联时，用于标记工作项当前所处的生命周期阶段，方便在看板中看进度。

---

## 三、如何创建工作流

进入「工作流」页面，点击「新建工作流」填写名称和描述后，进入可视化编辑器，按以下步骤搭建：

### 步骤 1：拖放节点

从编辑器左侧的**节点面板**中，把需要的节点拖拽到中间画布上。先搭好整体骨架：一个 Start、中间的执行 / 审批 / 条件节点、以及一个或多个结束节点。

### 步骤 2：连线

把鼠标移到节点边缘的**连接点（小圆点）**上，按住并拖到目标节点，即可建立一条连线，表示执行顺序。条件节点、并行节点可以拉出多条连线，形成分支。

### 步骤 3：配置节点属性

点击任意节点，编辑器右侧会弹出**属性配置面板**，填写该节点的具体参数（详见第六节「节点配置详解」）。修改后会实时反映到画布上（如节点名称同步更新）。

### 步骤 4：保存

点击「保存」按钮完成创建。保存时系统会自动校验：

- 必须有且仅有一个 Start 节点；
- 必须至少有一个结束节点（End / Cancel / Error / Close）；
- 结束节点不能再往外连线；
- 所有节点都要能从 Start 到达（不能有"孤岛"节点）。

校验通过后保存成功，版本号会自动递增，方便追踪修改历史。

### 画布常用操作

- **选中**：点击节点或连线；
- **框选**：按住鼠标拖拽，一次选中多个节点；
- **缩放**：滚动鼠标滚轮；
- **平移**：按住空格或鼠标中键拖动画布；
- **删除**：选中后按 Delete / Backspace 键。

---

## 四、如何为项目绑定工作流

工作流搭建好后，需要与工作项 / 项目关联才能自动流转：

1. 在工作项流转过程中，当工作项进入某个特定阶段时，可触发关联的工作流；
2. 也可以通过「定时调度」在指定时间自动触发某条工作流；
3. 在工作流编辑页面还能**手动触发**一次运行，用于验证流程是否正确。

绑定后，工作流就会在对应的时机自动运行，无需每次手动发起。

---

## 五、工作流运行状态查看

### 运行中的节点状态

一次运行过程中，每个节点都有独立的状态：

| 状态             | 含义                         |
|------------------|------------------------------|
| 等待中           | 前置节点还没完成，尚未开始   |
| 执行中           | 节点正在运行                 |
| 已完成           | 节点执行成功                 |
| 失败             | 节点执行出错                 |
| 已跳过           | 因条件不满足被跳过           |
| 等待审批         | 停在审批节点，等待人工确认   |

### 查看运行历史

在工作流编辑页面的「运行历史」区域，可以查看：

- 每次运行的触发时间和完成时间；
- 整体运行结果（成功 / 失败 / 进行中）；
- 各个节点的执行状态和耗时；
- 每个节点的输入与输出详情，方便定位问题出在哪一环。

运行状态会实时刷新，无需手动刷新页面。

---

## 六、节点配置详解

### Agent 节点

- **节点名称**：给节点起一个能体现职责的名字，如"代码生成""代码审查"；
- **模型**：选择该节点使用的 AI 模型；
- **提示词**：用自然语言描述这个节点要完成的任务，可引用上游节点的输出内容。

建议每个 Agent 节点只承担单一明确的职责，复杂任务拆成多个节点。

### 审批节点

- **节点名称**：审批环节的名称，如"技术评审"；
- **审批人**：指定谁来审批。

审批通过后流程继续；也可以配合条件节点实现"通过 / 拒绝"走不同分支。

### 条件节点

- **判断字段**：要检查的上下文字段；
- **比较方式**：等于、不等于、包含、大于、小于、大于等于、小于等于等；
- **比较值**：用来对比的目标值。

配置后请为它拉出满足和不满足两条出线，确保任何情况都有明确走向。

### 合并节点（Git Merge）

- **源分支**：要被合并的分支（留空则自动使用工作项分支）；
- **目标分支**：合并到哪个分支（留空则自动使用工作项分支）；
- **合并策略**：
  - 标准合并（merge）：保留完整提交历史；
  - 压缩合并（squash）：把多次提交压缩为一个，保持目标分支整洁；
  - 变基（rebase）：变基到目标分支，保持线性历史；
- **冲突处理**：遇到冲突时是直接失败还是尝试解决；
- **是否删除源分支**、**是否自动推送**：按需勾选。

### 并行节点

并行分发和并行汇合一般成对使用：先用 Parallel 拉出多条并行路径，再用 Parallel Join 等所有路径完成后汇合。无需额外参数，主要靠连线组织结构。

### 延时节点

- **等待秒数**：设置暂停多久后继续。

---

## 七、如何复制工作流

当你想基于一个现有工作流快速搭建相似流程时，可以直接复制：

1. 在工作流列表中找到目标工作流；
2. 点击其操作菜单中的「复制」；
3. 系统会创建一个包含相同节点和连线的副本；
4. 打开副本，按需修改节点配置后另存即可。

复制适合沉淀"流程模板"：把常用流程做好后复制出多个变体，避免每次从零搭建。

---

## 八、最佳实践

### 流程设计

- 每个工作流保持职责清晰，避免把太多逻辑堆在一起；
- 需要分情况处理时，用条件节点分支，而不是拆成多个独立工作流；
- 给每个节点起清晰的名称，方便在运行历史中快速识别。

### 并行提效

- 相互独立的子任务尽量用并行节点同时执行，缩短整体耗时；
- 避免不必要的串行等待。

### 审批与安全

- 把审批节点放在关键决策点前（如合并前、发布前）；
- 重要操作可用条件节点做双重校验；
- 善用 Error / Cancel 等不同结束节点，区分异常退出的原因。

### Git 合并

- 合并前确认源分支、目标分支配置正确；
- 留空分支字段时要理解默认行为（使用工作项分支）；
- 合并策略按需选择：日常开发用标准合并，特性分支用压缩合并，追求线性历史用变基；
- 建议在合并节点前加一个审批节点做人工把关。

### 调试技巧

- 工作流保存后先手动触发一次试运行，确认流程走通；
- 通过运行历史查看各节点输入输出，快速定位问题环节；
- 长流程可在中间加延时节点作为观察断点。

---

> 工作流把零散的 AI 步骤和人工环节编织成一条可靠的自动化流水线。先理清流程走向，再逐个配置节点，多用运行历史复盘，就能让复杂协作稳定高效地跑起来。
`;
