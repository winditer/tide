export const WORKFLOWS_GUIDE = `# 工作流（Workflows）使用指南

## 一、概述

工作流（Workflows）是 Tide 提供的**基于有向无环图（DAG）的多 Agent 协作编排引擎**，用于将多个 AI 执行节点、人工审批、条件路由和 Git 操作组合为自动化流程。

与单次 Agent 对话或简单 Plan 不同，工作流支持：

- **复杂的控制流**：条件分支、并行执行、延时等待；
- **人工介入**：在关键节点设置审批门禁；
- **Git 集成**：自动合并分支、处理冲突；
- **多终态**：区分正常完成、取消、错误、业务关闭等退出语义。

典型应用场景：

- 代码审查流水线：Agent 生成代码 → 审批 → 合并分支；
- 多 Agent 协作开发：前端、后端、测试并行执行；
- 审批与分支管理：代码完成后自动发起审批再合并；
- 自动化运维：定时触发巡检工作流，条件路由告警。

### 核心概念

- **节点（Node）**：工作流中的一个执行单元，如 Agent、审批、条件分支等；
- **边（Edge）**：节点之间的有向连线，定义执行顺序和数据流向；
- **运行（Run）**：工作流的一次执行实例，记录每个节点的状态和输出；
- **上下文（Context）**：运行时在节点间传递的数据载体。

### 与 Plan / Schedule 的关系

| 能力       | Plan             | Workflow                      | Schedule         |
|------------|------------------|-------------------------------|------------------|
| 编排方式   | 线性/并行任务列表 | DAG 图（可视化）              | 定时触发器       |
| 控制流     | 仅 max_parallel  | 条件/并行/延时/审批           | 无               |
| 人工介入   | 不支持           | Approval 节点                 | 不支持           |
| Git 操作   | 不支持           | Git Merge 节点                | 不支持           |
| 触发方式   | 手动/API         | 工作项/定时/手动              | 定时自动         |

---

## 二、节点类型详解

### 控制流节点

工作流使用特殊节点来标记流程的开始与结束。

| 节点     | 图标 | 说明                             |
|----------|------|----------------------------------|
| Start    | ▶    | 工作流唯一入口，每个工作流必须有且仅有一个 |
| End      | ■    | 正常完成终态，表示工作流成功结束 |
| Cancel   | ⊘    | 取消中止终态，表示流程被主动取消 |
| Error    | ✕    | 错误终止终态，表示发生异常退出   |
| Close    | ○    | 业务关闭终态，表示业务层面的关闭 |

> 提示：一个工作流可以有多个终态节点（End / Cancel / Error / Close），根据不同分支走向不同的结束语义。终态节点不能有出边。

### 执行节点

#### Agent (🤖)

工作流核心执行单元，调用 AI Agent 完成具体任务。

配置项：

- **label**：节点显示名称
- **model**：使用的 AI 模型（如 \`claude-sonnet-4-20250514\`、\`gpt-4o\`）
- **prompt**：传给 Agent 的提示词模板
- **routingTrigger**：智能路由触发标识（可选）

使用建议：

- 每个 Agent 节点应有明确单一的职责
- prompt 中可引用上游节点的输出变量
- 对于复杂任务，拆分为多个 Agent 节点串行或并行执行

#### Approval (🛡)

人工审批节点，暂停工作流执行等待人工确认。

配置项：

- **label**：审批环节名称
- **approvers**：审批人列表

使用建议：

- 放在关键决策点前（如代码合并、正式发布）
- 审批通过后流程继续；拒绝则走向 Error 或其他分支
- 可结合 Condition 节点实现"审批通过/拒绝"的条件路由

#### Git Merge (🔀)

自动执行 Git 分支合并操作。

配置项：

- **label**：节点显示名称
- **sourceBranch**：源分支（留空则自动使用工作项分支）
- **targetBranch**：目标分支（留空则自动使用工作项分支）
- **mergeStrategy**：合并策略
- **deleteSource**：合并后是否删除源分支
- **onConflict**：冲突处理策略（fail / resolve）
- **autoPush**：合并后是否自动推送

合并策略说明：

| 策略     | 说明                                           |
|----------|------------------------------------------------|
| merge    | 标准合并，保留所有提交历史，生成合并节点       |
| squash   | 将所有提交压缩为一个，保持目标分支线性         |
| rebase   | 变基到目标分支上，保持线性历史                 |

> 当 sourceBranch 和 targetBranch 都留空时，默认使用工作项的 worktree 分支（\`tide/wi-{id}\`）作为目标。

### 路由节点

#### Condition (◆)

条件分支节点，根据运行时上下文的字段值决定走哪条出边。

配置项：

- **label**：节点显示名称
- **field**：要判断的上下文字段路径
- **operator**：比较操作符
- **value**：比较目标值

支持的操作符：

| 操作符   | 说明       | 示例                    |
|----------|------------|-------------------------|
| eq       | 等于       | \`status eq "approved"\`  |
| ne       | 不等于     | \`result ne "fail"\`      |
| contains | 包含       | \`tags contains "urgent"\` |
| gt       | 大于       | \`score gt 80\`           |
| lt       | 小于       | \`errors lt 5\`           |
| gte      | 大于等于   | \`coverage gte 80\`       |
| lte      | 小于等于   | \`risk lte 3\`            |

> Condition 节点至少需要两条出边：一条满足条件时走，另一条不满足时走（默认分支）。

#### Parallel (+)

并行分发节点（Fork），将流程拆分为多条并行路径同时执行。

- 从该节点出发的所有出边对应的目标节点会同时开始执行
- 通常与 Parallel Join 配对使用

#### Parallel Join (−)

并行汇合节点（Join），等待所有并行分支执行完毕后继续。

- 会等待所有入边的上游节点全部完成
- 所有并行分支的输出会合并到上下文中

### 其他节点

#### Delay (⏱)

延时等待节点，暂停指定时间后继续执行。

配置项：

- **seconds**：等待秒数

使用场景：

- 等待外部系统处理完成（如 CI/CD 构建）
- 在重试场景中加入退避延时
- 控制执行节奏避免频率过高

#### Stage (✦)

工作项阶段标记节点，用于标记工作项的生命周期阶段。

- 当工作流与工作项关联时，经过此节点会更新工作项的阶段状态
- 可用于在看板中可视化工作项的进度

---

## 三、工作流编辑器

### 画布操作

工作流编辑器提供可视化的 DAG 编排能力：

- **添加节点**：从左侧节点面板拖拽节点到画布
- **连接节点**：从节点端口（小圆点）拖出连线到目标节点
- **选中节点**：点击节点，右侧显示属性配置面板
- **框选**：按住鼠标拖拽选择多个节点
- **缩放**：滚轮缩放画布
- **平移**：按住空格或中键拖拽平移画布
- **删除**：选中节点或连线后按 Delete/Backspace 删除

### 属性配置

选中任意节点后，右侧会弹出属性面板（PropertyPanel）：

- 各节点类型有对应的配置字段集
- Agent 节点可配置模型选择、提示词模板
- Git Merge 节点可配置分支名、合并策略
- Condition 节点可配置判断条件

配置修改后会实时反映到画布上（节点 label 等会同步更新）。

### 保存与版本

- **保存**：点击保存按钮，版本号（version）自动递增
- **保存验证**：
  - 必须有且仅有一个 Start 节点
  - 必须有至少一个终态节点（End / Cancel / Error / Close）
  - 终态节点不能有出边
  - 所有节点必须可达（从 Start 能到达）
- **版本追踪**：每次保存会生成新版本，可查看历史版本

---

## 四、常见工作流模式

### 模式 1: 简单 Agent 链

最基础的串行执行模式：

\`\`\`
Start → Agent(代码生成) → Agent(代码审查) → End
\`\`\`

适用于：步骤明确、前后依赖的线性任务。

### 模式 2: 条件分支

根据执行结果进行路由：

\`\`\`
Start → Agent(分析) → Condition(是否通过?)
  → 通过: End
  → 未通过: Agent(修复) → End
\`\`\`

适用于：需要根据 Agent 输出做不同处理的场景。

### 模式 3: 并行执行

多任务同时执行，提升效率：

\`\`\`
Start → Parallel → Agent(前端开发)  ┐
                 → Agent(后端开发)  ├→ Parallel Join → End
                 → Agent(测试用例)  ┘
\`\`\`

适用于：多个独立子任务可以同时进行的场景。

### 模式 4: 审批工作流

加入人工审批环节：

\`\`\`
Start → Agent(开发) → Approval(技术评审) → Condition(批准?)
  → 批准: Git Merge → End
  → 拒绝: Error
\`\`\`

适用于：需要人工确认后才能继续的关键流程（代码合并、版本发布等）。

### 模式 5: Git 集成流程

完整的代码开发 → 合并流程：

\`\`\`
Start → Agent(编码) → Agent(自测) → Git Merge(合并到 release) → End
\`\`\`

适用于：自动化的开发分支合并流程。

### 模式 6: 复合模式

结合多种节点构建复杂流程：

\`\`\`
Start → Parallel → Agent(前端)   ┐
                 → Agent(后端)   ├→ Parallel Join → Approval(评审)
                 → Agent(文档)   ┘
  → Condition(批准?)
    → 批准: Git Merge → End
    → 拒绝: Agent(修复) → Approval(复审) → ...
\`\`\`

适用于：企业级多人协作、多环节流程。

---

## 五、运行与调试

### 触发方式

工作流可以通过以下方式触发执行：

- **工作项流转**：当工作项进入特定阶段时自动触发关联的工作流
- **定时调度**：通过 Schedule（task_type: workflow）在指定时间触发
- **手动触发**：在工作流编辑页面或通过 API 手动发起一次运行

### 运行状态

工作流运行中，每个节点有独立的状态：

| 状态              | 说明                       |
|-------------------|----------------------------|
| pending           | 节点等待执行（前置未完成） |
| running           | 节点正在执行中             |
| completed         | 节点执行成功完成           |
| failed            | 节点执行失败               |
| skipped           | 节点被跳过（条件不满足）   |
| waiting_approval  | 等待人工审批确认           |

### 运行上下文

每次运行都有一个上下文（Context）对象：

- Start 节点可以接收初始输入上下文
- 每个节点执行完毕后，其输出会写入上下文
- 下游节点可以通过字段引用访问上游输出
- Condition 节点基于上下文字段做判断

### 查看运行历史

在工作流编辑页面底部的"运行历史"区域可以查看：

- 每次运行的触发时间和完成时间
- 整体运行状态（成功/失败/进行中）
- 各节点的执行状态和耗时
- 节点的输入输出详情

---

## 六、最佳实践

### 设计原则

- 每个工作流**必须有一个 Start** 和**至少一个终态节点**
- 保持单个工作流职责明确，避免过度复杂
- 复杂流程使用 Condition 节点做分支，而非创建多个独立工作流
- 合理命名每个节点的 label，便于在运行历史中识别

### 性能优化

- 使用 Parallel / Parallel Join 提升并行执行效率
- 独立的子任务尽量并行化
- 避免不必要的串行等待

### 审批与安全

- 审批节点放在关键决策点（如代码合并前、正式发布前）
- 对重要操作使用 Condition 做双重验证
- 合理使用 Error / Cancel 终态区分异常退出类型

### Git 操作

- Git Merge 节点需确保 sourceBranch 和 targetBranch 正确配置
- 留空分支字段时理解默认行为（自动使用工作项分支）
- 合并策略选择：日常开发用 merge，特性分支用 squash，保持线性用 rebase
- 建议在 Git Merge 前加入 Approval 节点做人工把关

### 调试技巧

- 工作流保存后可在编辑器中手动触发试运行
- 查看运行历史中各节点的输入输出，定位问题环节
- 对于长流程，可以在中间节点加 Delay 作为断点观察
- 善用多终态区分不同的退出原因，便于后续分析

---

## 七、API 接口

所有接口前缀为 \`/api/workflows\`。

| 方法     | 路径                                    | 说明                        |
|----------|-----------------------------------------|-----------------------------|
| \`POST\`   | \`/api/workflows\`                        | 创建工作流                  |
| \`GET\`    | \`/api/workflows\`                        | 获取工作流列表              |
| \`GET\`    | \`/api/workflows/{id}\`                   | 获取工作流详情（含节点/边） |
| \`PUT\`    | \`/api/workflows/{id}\`                   | 更新工作流（保存画布）      |
| \`DELETE\` | \`/api/workflows/{id}\`                   | 删除工作流                  |
| \`POST\`   | \`/api/workflows/{id}/run\`               | 手动触发一次运行            |
| \`GET\`    | \`/api/workflows/{id}/runs\`              | 获取运行历史列表            |
| \`GET\`    | \`/api/workflows/{id}/runs/{run_id}\`     | 获取单次运行详情            |

### 创建工作流示例

\`\`\`bash
curl -X POST http://localhost:8000/api/workflows \\
  -H "Content-Type: application/json" \\
  -d '{
    "name": "代码审查流水线",
    "description": "自动代码生成 → 审查 → 合并"
  }'
\`\`\`

### 手动触发运行示例

\`\`\`bash
curl -X POST http://localhost:8000/api/workflows/{id}/run \\
  -H "Content-Type: application/json" \\
  -d '{
    "input_context": {
      "branch": "feature/new-api",
      "task": "实现用户注册接口"
    }
  }'
\`\`\`

### 查询运行历史示例

\`\`\`bash
curl "http://localhost:8000/api/workflows/{id}/runs?limit=20"
\`\`\`

> 工作流运行状态变更会通过 WebSocket（\`workflows\` 频道）实时推送，前端编辑器和列表页会自动刷新。
`;
