{
  "name": "tide",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "yarn workspace @tide/web dev",
    "build": "yarn workspace @tide/web build",
    "lint": "yarn workspace @tide/web lint"
  }
}
